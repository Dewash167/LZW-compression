typedef struct{
    int prefix; 
    int character; 
} DictElement;                   

void dArrayAdd(int prefix, int character, int value);
int dArrayPrefix(int value);
int diArrayCharacter(int value);
